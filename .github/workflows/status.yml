name: ci

on:
  push:
    branches:
      - ci
  schedule:
    - cron: "0 9 * * *"

jobs:
  autogreen:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v2

      - name: Check Status
        run: |
          git config --local user.email "1344299971@qq.com.com"
          git config --local user.name "kortin99"
          git remote set-url origin https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          git pull --rebase
          git commit --allow-empty -m "ci: check status"
          git push
